{
  "name": "japan2026-site",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "presync": "node scripts/sync-content.mjs",
    "sync": "node scripts/sync-content.mjs",
    "dev": "npm run sync && astro dev",
    "build": "npm run sync && astro build",
    "preview": "astro preview"
  },
  "dependencies": {
    "@vite-pwa/astro": "^0.5.0",
    "astro": "^5.0.0"
  },
  "devDependencies": {
    "sharp": "^0.33.5"
  }
}
